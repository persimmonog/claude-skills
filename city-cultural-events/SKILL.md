---
name: city-cultural-events
description: 当用户询问某个城市的文化活动推荐时使用此 skill，包括展览、演出、Livehouse、话剧、音乐会、脱口秀、音乐节、书店活动等线下活动。触发场景：用户提到城市名 + 活动/演出/展览/周末去哪儿/同城活动等关键词；用户询问本地文化娱乐推荐；用户想要按活动类型筛选。数据来自大麦网 mtop API、豆瓣同城和活动行。即使只提到城市名也可以主动询问偏好并推荐。
---

# 同城文化活动推荐 Skill

## 工作流程

### 1. 确认用户需求

从用户输入中提取以下信息，缺少则主动询问：

| 信息 | 说明 | 示例 |
|------|------|------|
| 城市 | 必填 | 北京、上海、杭州 |
| 活动类型 | 可选，默认全部 | 展览、演唱会、话剧、脱口秀、音乐会 |
| 数量 | 可选，默认 20 | 推荐 10 个、最多 30 个 |

### 2. 抓取活动数据

使用 bundled 脚本从大麦网、豆瓣同城和活动行获取实时活动数据：

```bash
python3 <skill-dir>/scripts/scrape_events.py <城市> [活动类型] [数量] [--format json|markdown]
```

**示例**：
- `python3 scripts/scrape_events.py 北京` — 获取北京全部活动，最多 20 条（JSON 输出）
- `python3 scripts/scrape_events.py 上海 展览 15 --format markdown` — 获取上海展览类活动，Markdown 表格输出

**活动类型参数**：全部（默认）、演唱会、话剧、音乐剧、音乐会、Livehouse、展览、脱口秀

> **注意**：脚本依赖 Playwright（`pip3 install playwright && playwright install chromium`）。如果环境没有安装，先安装再运行。

### 3. 解析脚本输出

脚本返回 JSON 格式，结构如下：

```json
{
  "city": "北京",
  "type": "全部",
  "search_date": "2026-04-29",
  "total": 20,
  "categories": {
    "演唱会/音乐节": [...],
    "话剧/音乐剧": [...],
    "展览": [...],
    "脱口秀/喜剧": [...],
    "其他": [...]
  }
}
```

每个活动包含：`title`（名称）、`date`（时间）、`venue`（场馆）、`price`（票价）、`tags`（标签/评分）、`ranking`（排行榜信息）、`link`（购票链接）

### 4. 格式化输出

使用 `--format markdown` 时，脚本直接输出 Markdown 表格：

```markdown
# {城市}文化活动推荐

> 搜索日期：{search_date} | 共推荐 **{total}** 场活动

## {分类名}

| 活动名称 | 时间 | 地点 | 票价 | 来源 | 链接 |
|----------|------|------|------|------|------|
| {活动名称} {排行榜标注} | {date} | {venue} | {price} | {platform} | [购票/详情]({link}) |
```

使用 `--format json`（默认）时，输出原始 JSON 数据。

### 5. 附加建议

在推荐列表后添加：
- **热门活动**：有排行榜信息（如"演唱会上新榜NO.1"）的活动优先推荐
- **价格区间**：同一分类内标注最低价和最高价
- **购票提示**：如有"可选座"、"限时早鸟"等标签则提醒

## 注意事项

- **数据时效性**：在推荐结果开头注明搜索日期，提醒用户活动信息可能变化
- **无结果处理**：如果某个分类没有找到活动，明确说明"该分类暂无活动"
- **信息准确性**：所有活动信息来自大麦网实时数据，不编造任何内容
- **链接有效性**：部分活动的购票链接可能为空（大麦 API 限制），此时仅展示名称供用户自行搜索
